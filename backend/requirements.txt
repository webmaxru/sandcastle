# Core: GitHub Copilot provider for Microsoft Agent Framework.
# Pin the exact pre-release version so `pip install -r` works WITHOUT --pre
# (avoids pulling alpha builds of transitive deps like pydantic).
agent-framework-github-copilot==1.0.0rc3

# Web / API
fastapi
uvicorn[standard]
sse-starlette
pydantic-settings
python-dotenv
httpx

# Observability (Phase 5): Azure Application Insights exporter for the
# OpenTelemetry traces/metrics/logs emitted by Agent Framework. Only used when
# APPLICATIONINSIGHTS_CONNECTION_STRING is set; harmless otherwise.
azure-monitor-opentelemetry-exporter
